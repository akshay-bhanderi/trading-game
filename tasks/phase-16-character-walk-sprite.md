# Phase 16 — New Character Sprite + Real Walk Animation

[← Back to index](../TASK.md)

Status: **Complete (T073).**

> Not part of the original design doc's build order — user-supplied replacement art for the hub-scene character (`src/ui/scene/character.ts`, T069). Two problems being fixed at once: (1) the current CraftPix "Free City Trader" pack is being swapped out entirely for a new suit-and-tie character the user provided, and (2) the current "walk" behavior is fake — see `character.ts`'s own file-header comment: the CraftPix pack has no walk-cycle sheet, so today's `'walk'` state just translates the **idle** pose across the floor (`animations.idleAlt`), which reads as crawling/sliding rather than walking. The new art includes a real walk-cycle sheet, so this task both re-skins the character and wires up genuine walking.
>
> **Resolved (2026-08-01):** built as planned, with two deviations. (1) The Downloads folder actually contained *two* walk sheets — `walk-right-to-left.png` and `walk-left-to-right.png` — not one path listed twice as the task doc assumed. Pixel-diffed them: both show the character in the same left-facing profile (not mirrored), just a different generation pass, so only `walk-right-to-left.png` was used (copied in as `walk.png`), matching the task's "mirror the single sheet via `scale.x`" plan; `walk-left-to-right.png` was left untouched in Downloads, not copied into the repo. (2) Frame counts were re-verified independently via an alpha-channel column scan (not trusted from the doc): confirmed 15 walk frames / 14 idle frames, matching the doc's numbers exactly. Sprite scale was changed from the old `1.1` to `0.65` — the new sheets are 170px-tall portraits vs. the old 128×128 square frames, and content-bounding-box measurement (idle frame's visible pixels span ~147 of the 170px height, vs. ~66 of 128px on the old CraftPix frame) showed `1.1` would have rendered the character more than 2x its old apparent size next to the room's fixed-size props (e.g. the 64px wall window); `0.65` was chosen after mocking up the composite scene layout in a throwaway script to sanity-check proportions, since no browser was available for a live screenshot.

- [x] **T073 — Swap in the new walk/idle character art and drive real walk-cycle animation**
  - Doc references: §12 (hub scene character), and `character.ts`'s own header comment (documents the current fake-walk workaround this task removes)
  - Dependencies: T069 (existing character controller this replaces)
  - Source files (user-supplied, not yet in the repo — copy them in as part of this task):
    - `C:\Users\DELL\Downloads\walk-right-to-left.png` → measured 2070×170px, **15 frames** (content bounding boxes found via alpha-channel scan; frame pitch is ~134–152px, not a perfectly even grid — it's a hand-packed sheet, not machine-generated). Re-verify frame count by opening the file before slicing; don't trust this number blindly.
    - `C:\Users\DELL\Downloads\idle.png` → measured 1978×170px, **14 frames** (~141px pitch, same caveat).
    - Note: the user's original message listed the walk file path twice — there is only one walk sequence, not two. There is no separate "walk left" vs "walk right" sheet; direction is handled by mirroring (see below).
    - Copy both into `public/assets/character/` (e.g. `walk.png`, `idle.png`).
  - Slicing approach: unlike the old CraftPix sheets (fixed 128×128 square grid, see `FRAME_SIZE` in `character.ts`), these new sheets are **not square** (170px tall, ~138–141px wide per frame) and the grid isn't perfectly even. Slice each sheet into `frameCount` equal-width columns (`sheetWidth / frameCount`, full 170px height) rather than reusing the old fixed-`FRAME_SIZE` constant — a few px of empty margin per frame from the uneven pitch is imperceptible in an `AnimatedSprite` and not worth a manual per-frame crop.
  - Rewrite `src/ui/scene/character.ts`:
    - `CharacterAnimations` becomes `{ idle: Texture[]; walk: Texture[] }` — drop `idleAlt` and `wave` entirely (the new asset has no wave frames, and the user confirmed dropping the wave-idle behavior rather than mixing in the old CraftPix wave sheet).
    - `createCharacterController`'s `'walk'` state must play `animations.walk` (real walk-cycle) instead of `animations.idleAlt` — this is the core fix for the crawl-not-walk bug.
    - Direction: keep the existing `scale.x` sign-flip technique (`walkTargetX >= sprite.position.x ? 1 : -1`) to mirror the single walk sheet for rightward movement, same as today — confirmed approach, no separate right-facing sheet needed.
    - Remove `enterWave`/`WAVE_CHANCE`/all `wave` references; the idle↔walk toggle in `update()` simplifies to just picking `enterWalk()` when the idle timer elapses (no wave branch).
    - Sprite anchor/scale: old frames were 128×128 square; new frames are a taller, narrower portrait (~138–141 × 170). Check the character's on-screen size still reads sensibly next to the existing hub-scene props after the swap (compare footprint against the old sprite) — adjust `sprite.scale` if it renders too large/small, since the source pixel dimensions changed.
  - `HubScene.tsx` calls `loadCharacterAnimations`/`createCharacterSprite`/`createCharacterController` (around line 217) — check for any assumptions tied to the old animation shape (e.g. nothing should reference `.wave` or `.idleAlt` after this change).
  - Delete the old CraftPix assets (per user decision — full replacement, not a fallback): `public/assets/character/trader-idle.png`, `trader-idle-2.png`, `trader-wave.png`, `LICENSE.txt`.
  - Note: the new art has no license/attribution documentation in this repo (unlike the CraftPix pack's `LICENSE.txt`). Not this task's job to fabricate one — flag it, don't invent a license file.
  - Acceptance criteria: hub-scene character renders as the new suited sprite; idles on `idle.png`'s frames; when it walks, it visibly cycles through real walking-leg frames (not a static pose sliding across the floor) and mirrors correctly for both travel directions; old CraftPix files removed from the repo; no dead code referencing `wave`/`idleAlt`.
  - Mobile/desktop note: same as T070/T069 — verify the sprite doesn't look stretched/squashed at whatever `sprite.scale` ends up being, on both a narrow mobile viewport and desktop.
