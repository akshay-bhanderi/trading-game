node_modules
dist
dist-ssr
*.local
*.tsbuildinfo
.vscode/*
!.vscode/extensions.json
.DS_Store

# Raw itch.io asset download (PSDs, coupon art, unused trader variants) —
# only the specific PNG frames we actually use are committed, under
# public/assets/character/. See public/assets/character/LICENSE.txt.
/assets-src/
# ...except our own icon/OG sources, which scripts/generate-icons.mjs reads.
!/assets-src/icon/
!/assets-src/icon/**
