node_modules
dist
dist-ssr
*.local
*.tsbuildinfo
.vscode/*
!.vscode/extensions.json
.DS_Store

# Raw asset source downloads (PSDs, unused variants) — only the specific
# PNG frames actually used are committed, under public/assets/character/.
# (T073: the character pack is now user-supplied art with no license/
# attribution file in the repo — the old CraftPix LICENSE.txt this comment
# used to point to was removed along with that pack.)
/assets-src/
# ...except our own icon/OG sources, which scripts/generate-icons.mjs reads.
!/assets-src/icon/
!/assets-src/icon/**
